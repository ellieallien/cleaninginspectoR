---
title: "Quickstart"
author: "Eliora Henzler"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Detailed Example}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```


```{r, results='hide', message=FALSE,warning=FALSE,error=FALSE}
library("cleaninginspectoR")
```


## Example data frame
For illustration purposes
```{r}
testdf <- data.frame(a= c(runif(98),7287,-100),
                   b=sample(letters,100,T),
                   uuid=c(1:98, 4,20),
                   water.source.other = c(rep(NA,98),"neighbour's well","neighbour's well"),
                   GPS.lat = runif(100)
                   )
```

## Apply all cleaning inspections

```{r}
inspect_all(testdf)
```


## If the uuid id column does not contain "uuid", specify:

```{r}
inspect_all(df = testdf,uuid.column.name = "b")
```
